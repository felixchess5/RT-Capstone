# Minimal demo UI environment (Gradio) without spaCy stack

# Gradio UI (keep separate from spaCy/weasel due to Typer constraints)
gradio==4.44.1

# Network/runtime dependencies commonly required by Gradio
aiohttp==3.12.15
yarl==1.20.1
frozenlist==1.5.0
multidict==6.1.0
aiosignal==1.4.0
aiofiles==23.1.0
httpx==0.28.1
httpcore==1.0.9
anyio==4.6.2.post1
sniffio==1.3.1

# Core utility libs
packaging==24.1
typing-extensions==4.12.2
tqdm==4.67.1

# Requests family
requests==2.32.3
urllib3==2.2.3
certifi==2025.8.3
charset-normalizer==3.4.2

# Optional: HF Hub (often pulled indirectly by components)
huggingface-hub==0.33.5

# NOTE: No spaCy/weasel/typer pins here to avoid conflicts.
# The demo should call backend APIs rather than import NLP processors directly.
